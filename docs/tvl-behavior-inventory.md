# TVL Behavior Inventory

This inventory captures current TVL inclusion behavior before extracting shared TVL helpers or adding broader abstractions.

Do not treat shared toggle names as proof that two routes use the same model. Current code has at least four distinct TVL behaviors: adjusted chain base TVL, chain chart add-back, protocol table add-back with `strikeTvl`, and protocol overview chart composition.

## Toggle Terms

Shared TVL setting keys live in `src/contexts/LocalStorage.tsx`:

- `staking`
- `pool2`
- `borrowed`
- `doublecounted`
- `liquidstaking`
- `vesting`
- `govtokens`

Related but separate keys:

- `dcAndLsOverlap` is not a setting key. It is fetched or carried as an overlap adjustment when both `doublecounted` and `liquidstaking` are enabled.
- `excludeParent` and `excludeparent` are table/read-model fields, not TVL toggles.
- `offers` appears in some upstream TVL maps and is filtered out in protocol/category paths.

Relevant local storage namespaces:

- `tvl`: main TVL toggle settings used by chains, protocol ranking/list surfaces, taxonomy pages, oracles, forks, compare pages, and watchlist.
- `tvl_fees`: protocol overview surfaces where TVL extras and fee extras share a filter set.
- `tvl_chains`: chain grouping controls, not TVL inclusion settings.

## Behavior Models

| Area                                           | Route family                                                                              | Source files                                                                                                                                                                                                                                                            | Base model                                                                                                                                                                                                                                                                                                                             | Extra model                                                                                                                                                                                                                                                                                                            | Overlap handling                                                                                                                                                                                                                                                                                                                                                                                                                                                     | Existing tests                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   | Share decision                                                                                                                                                                                                                |
| ---------------------------------------------- | ----------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Shared adjusted TVL utilities                  | Server/pro dashboard adjusted TVL consumers                                               | `src/utils/tvl.ts`, `src/server/protocolSplit/*`, `src/containers/ProDashboard/services/*`                                                                                                                                                                              | Adjusted base is `base - doublecounted - liquidstaking + dcAndLsOverlap`. Protocol helper ignores `staking`, `pool2`, `borrowed`, and `vesting` while accumulating base.                                                                                                                                                               | Not a UI toggle model; it derives adjusted chart series.                                                                                                                                                                                                                                                               | Always adds overlap back as part of adjusted-base calculation.                                                                                                                                                                                                                                                                                                                                                                                                       | `src/utils/__tests__/tvl.test.ts` covers base adjustment, ignored extra sections, scoped chain adjustment, and current-day filtering.                                                                                                                                                                                                                                                                                                                                                                                                                                                                            | Do not merge with table or chart toggle models.                                                                                                                                                                               |
| Chain directory/category pages                 | `/chains`, `/chains/[category]`                                                           | `src/containers/ChainsByCategory/queries.tsx`, `src/containers/ChainsByCategory/index.tsx`, `src/containers/ChainsByCategory/tvl.ts`, `src/pages/api/public/page-data/chains/charts.ts`                                                                                 | Server normalizes chain row base values with `normalizeChainsBaseTvlValue`, after stale extra-TVL cleanup.                                                                                                                                                                                                                             | Client applies selected extras with `applyChainsTvlSettings` to row values, dominance charts, and pie data. Chart API receives selected `extraTvlTypes`.                                                                                                                                                               | Subtracts `dcAndLsOverlap` when both `doublecounted` and `liquidstaking` are enabled. Stale overlap is removed unless both overlap parents remain.                                                                                                                                                                                                                                                                                                                   | `src/containers/ChainsByCategory/__tests__/tvl.test.ts` covers base adjustment, add-back, null behavior, and stale extra cleanup.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | This is the clearest chain TVL model. Share only with other chain surfaces after chart behavior tests prove parity.                                                                                                           |
| Chain overview and compare-chain charts        | `/chain/[chain]`, `/chart/chain/[chain]`, `/compare-chains`                               | `src/containers/ChainOverview/useFetchChainChartData.tsx`, `src/containers/ChainOverview/tvlChart.ts`, `src/containers/CompareChains/index.tsx`, `src/containers/CompareChains/tvlChart.ts`                                                                             | Starts from the provided chain `tvlChart`; when no extras are enabled, ChainOverview uses server summary values, while CompareChains reuses the base chart series for its comparison chart. CompareChains summary cards are rendered by ChainOverview `Stats`, so their TVL values still follow the ChainOverview summary/toggle path. | Adds enabled extra chart values into each base chart timestamp through the shared ChainOverview chart settings helper.                                                                                                                                                                                                 | Subtracts `dcAndLsOverlap` when both `doublecounted` and `liquidstaking` are enabled. Missing overlap subtracts zero. No stale-extra cleanup is performed locally in these chart functions.                                                                                                                                                                                                                                                                          | `src/containers/ChainOverview/__tests__/chain-chart-page.test.tsx` verifies extra chart prop pass-through. `src/containers/ChainOverview/__tests__/tvlChart.test.ts` covers ChainOverview summary bypass, add-back, overlap subtraction, missing overlap, and gov-token state. `src/containers/CompareChains/__tests__/tvlChart.test.ts` covers CompareChains base chart passthrough, add-back, overlap subtraction, missing overlap, one-parent overlap, and gov-token add-back.                                                                                                                                | ChainOverview and CompareChains now share only the chain chart settings transform. Keep ChainOverview summary behavior local, and do not merge this helper with category stale-extra cleanup or protocol timestamp alignment. |
| Protocol overview main TVL chart               | `/protocol/[protocol]`, `/chart/protocol/[protocol]`                                      | `src/containers/ProtocolOverview/useFetchProtocolChartData.ts`, `src/containers/ProtocolOverview/queries.client.tsx`, `src/containers/ProtocolOverview/tvlChart.ts`                                                                                                     | Starts from prefetched or fetched protocol base TVL chart.                                                                                                                                                                                                                                                                             | Extra charts are fetched/added only when `needsCompositeTvlChart`, `tvlSettings[key]`, and `currentTvlByChain?.[key] != null`. `staking` and `borrowed` follow their local fetch-state helper, so they can also fetch for standalone toggles. Current protocol page payloads do not expose chain-style overlap fields. | No explicit `dcAndLsOverlap` handling in `buildTvlChart`; `dcAndLsOverlap` is not a ProtocolOverview setting. Latest base timestamp shifts only when the most recent main/extra timestamp is within `MAX_TVL_TIMESTAMP_ALIGNMENT_DIFF_SEC` (`24 * 60 * 60`); if the latest extra is farther away, the base timestamp is retained but the latest extra value is still added to that latest base point. Denomination history drops points only when a rate is missing. | `src/containers/ProtocolOverview/__tests__/queries.client.test.tsx` covers query URL construction. `src/containers/ProtocolOverview/__tests__/tvlChart.test.ts` covers fetch gating on exposed TVL keys, composition, timestamp alignment behavior, denomination point dropping, and USD inflows.                                                                                                                                                                                                                                                                                                                | Keep TVL chart composition local to ProtocolOverview. Do not share with chain chart helpers without proving matching behavior.                                                                                                |
| Protocol overview key metrics and TVL-by-chain | `/protocol/[protocol]` summary cards                                                      | `src/containers/ProtocolOverview/helpers.ts`, `src/containers/ProtocolOverview/KeyMetrics.tsx`                                                                                                                                                                          | Aggregates `currentTvlByChain` by chain; keys with known extra suffixes become toggleable extras.                                                                                                                                                                                                                                      | Adds extra-suffixed chain values only when enabled in `tvl_fees`. Also exposes toggle options based on available TVL extras and `bribes`/`tokentax` fee extras.                                                                                                                                                        | No overlap-specific handling in `useFinalTVL`; do not fabricate `dcAndLsOverlap` as a protocol summary setting.                                                                                                                                                                                                                                                                                                                                                      | `src/containers/ProtocolOverview/__tests__/helpers.test.ts` covers `tvl_fees` namespace usage, enabled and disabled protocol TVL suffix extras, fee option exposure without TVL aggregation changes, and oracle TVS aggregation.                                                                                                                                                                                                                                                                                                                                                                                 | Local protocol overview summary behavior. Do not merge with chart composition without tests.                                                                                                                                  |
| Recent and airdrop protocol lists              | `/recent`, `/airdrops`, `/airdrop-directory`                                              | `src/containers/ProtocolLists/RecentProtocols.tsx`, `src/containers/ProtocolLists/utils.ts`                                                                                                                                                                             | Recalculates base TVL scoped to selected chains from protocol `chainTvls`.                                                                                                                                                                                                                                                             | `applyExtraTvl` adds enabled extras except `doublecounted` and `liquidstaking`; clamps negative values to zero; recalculates changes and `mcaptvl`.                                                                                                                                                                    | Explicitly does not apply chain doublecounted/liquidstaking subtraction.                                                                                                                                                                                                                                                                                                                                                                                             | `src/containers/ProtocolLists/__tests__/utils.test.ts` covers enabled extras, non-overlap behavior, negative clamping, percent changes, and `mcaptvl`.                                                                                                                                                                                                                                                                                                                                                                                                                                                           | Table/list-specific. Keep separate from chain overlap helpers.                                                                                                                                                                |
| Shared protocol ranking table                  | `/protocols`, chain overview protocol rows, watchlist, compare protocols                  | `src/containers/ProtocolRankings/Table.tsx`, `src/containers/ProtocolLists/utils.ts`, `src/containers/ProtocolRankings/utils.ts`, `src/containers/ProtocolRankings/readModel.ts`, `src/containers/DeFiWatchlist/index.tsx`, `src/containers/CompareProtocols/index.tsx` | Protocol rows carry a `tvl.default` entry plus keyed extra TVL entries. Dimension metric rows can be admitted without `total24h` when another relevant period, annualized, average, or all-time value exists. Parent DEX `change_7dover7d` is derived from aggregated totals rather than summed child percentages.                     | `applyProtocolTvlSettings` adds enabled extras except `doublecounted` and `liquidstaking`, handles child protocols, min/max filters, changes, and `mcaptvl`. Previous TVL fields may stay null after toggles; current TVL is still normalized for table display.                                                       | `toStrikeTvl` marks rows struck for removed categories or when row data exposes liquidstaking/doublecounted extra sections; `applyProtocolTvlSettings` clears existing `strikeTvl` when `doublecounted` or `liquidstaking` is enabled. Deprecated zero-TVL child protocols are excluded from parent previous-TVL change math, while non-deprecated zero-TVL children still count as real drops when previous TVL exists.                                             | `src/containers/ProtocolLists/__tests__/utils.test.ts` covers additive extras, non-additive overlap toggles, `strikeTvl` clearing, child rows, post-extra filtering, and null previous-TVL preservation. `src/containers/ProtocolRankings/__tests__/utils.test.ts` covers direct protocol/fork/oracle filtering and `toStrikeTvl`. `src/containers/ProtocolRankings/__tests__/readModel.test.ts` covers dimension metric admission and enrichment. `src/containers/ProtocolRankings/__tests__/queries.server.test.ts` covers parent DEX change derivation and deprecated/non-deprecated zero-TVL parent changes. | High blast radius shared table behavior. Keep covered before any extraction.                                                                                                                                                  |
| Protocol taxonomy pages                        | `/protocols/[category]`, `/protocols/[category]/[chain]`, `/categories`, `/top-protocols` | `src/containers/ProtocolTaxonomy/index.tsx`, `src/containers/ProtocolTaxonomy/CategoriesPage.tsx`, `src/containers/ProtocolTaxonomy/queries.tsx`, `src/containers/ProtocolTaxonomy/tvl.ts`                                                                              | Server filters out `doublecounted`, `liquidstaking`, `dcAndLsOverlap`, `offers`, and chain-suffixed extras in several category read models.                                                                                                                                                                                            | Client adds enabled extra TVL values into protocol/category rows and chart series through local ProtocolTaxonomy helpers. Categories page excludes a configured subset through `categoriesPageExcludedExtraTvls`.                                                                                                      | No chain-style overlap subtraction found in client add-back paths.                                                                                                                                                                                                                                                                                                                                                                                                   | `src/containers/ProtocolTaxonomy/__tests__/tvl.test.ts` covers category/tag row and chart extra add-back, direct doublecounted/liquidstaking addition without overlap subtraction, Lending borrowed chart mirroring, categories-page extra exclusions, selected-category chart add-back, and recursive table recalculation.                                                                                                                                                                                                                                                                                      | Route-family-specific taxonomy behavior. Keep the helpers local; do not merge with ProtocolRankings until tests identify shared invariants.                                                                                   |
| Oracles                                        | `/oracles*`                                                                               | `src/utils/tvlOverlap.ts`, `src/containers/Oracles/OracleOverview.tsx`, `src/containers/Oracles/queries.tsx`, `src/containers/Oracles/queries.client.tsx`                                                                                                               | Starts from raw oracle TVS/TVL values.                                                                                                                                                                                                                                                                                                 | Adds enabled extras to totals, table rows, and fetched extra chart series.                                                                                                                                                                                                                                             | Requests `dcAndLsOverlap` when both `doublecounted` and `liquidstaking` are enabled and subtracts it when calculating totals and extra chart series.                                                                                                                                                                                                                                                                                                                 | `src/utils/__tests__/tvlOverlap.test.ts` covers enabled API keys, overlap key gating, add-back, table/total overlap subtraction, and extra-series overlap sign behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                         | Shares only extra-TVL overlap helpers with Forks. Route-level chart/table assembly stays in Oracles.                                                                                                                          |
| Forks                                          | `/forks*`                                                                                 | `src/utils/tvlOverlap.ts`, `src/containers/Forks/tvl.ts`, `src/containers/Forks/ForksOverview.tsx`, `src/containers/Forks/queries.client.tsx`                                                                                                                           | Starts from raw fork TVL values.                                                                                                                                                                                                                                                                                                       | Adds enabled extras to table/chart data.                                                                                                                                                                                                                                                                               | Requests `dcAndLsOverlap` when both overlap parents are enabled and subtracts that series in overview/protocol chart helpers.                                                                                                                                                                                                                                                                                                                                        | `src/utils/__tests__/tvlOverlap.test.ts` covers enabled API keys, overlap key gating, add-back, table/total overlap subtraction, and extra-series overlap sign behavior.                                                                                                                                                                                                                                                                                                                                                                                                                                         | Shares only extra-TVL overlap helpers with Oracles. Fork-specific TVL/original percentage behavior remains local.                                                                                                             |

## Current Test Gaps

The highest-value characterization gaps from the initial inventory are now covered. Add new tests only when a specific refactor target or bug report needs them.

## Extraction Guidance

- Do not create `src/metrics/tvl.ts` from the current evidence. The repeated names are not yet stable cross-container invariants.
- The safest next production target is not broad extraction. ChainOverview and CompareChains chart behavior is locally covered, and only their shared chain add-back plus overlap transform is centralized.
- Keep ChainOverview summary behavior local. CompareChains only builds adjusted chart series for the comparison chart; its cards reuse ChainOverview `Stats` and therefore follow the ChainOverview summary/toggle path. Keep protocol overview timestamp alignment local.
- Oracles and Forks share the tiny `src/utils/tvlOverlap.ts` helper for enabled extra API keys, total add-back, and overlap sign handling. Do not pull chain/protocol table behavior into that helper.
